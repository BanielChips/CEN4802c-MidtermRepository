pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
                // Pull the code from your version control system (e.g., Git)
                git branch: 'main', url: 'https://github.com/BanielChips/CEN4802c-MidtermRepository.git'
            }
        }

        stage('Compile') {
            steps {
                // Compile the Java source files
                echo 'Running the JavaCalculator...'
                bat 'mvn exec:java -Dexec.mainClass="JavaCalculator"'
            }
        }

        stage('Run') {
            steps {
                // Run the compiled Java program (adjust according to your main class)
                bat 'java -cp bin JavaCalculator'  // Replace with your actual classpath and main class
            }
        }
    }

    post {
        always {
            // Optional: You can add any cleanup or notification steps here
            echo 'Pipeline execution completed.'
        }
        success {
            echo 'Build and Run succeeded!'
        }
        failure {
            echo 'Build or Run failed.'
        }
    }
}
